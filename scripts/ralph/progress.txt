## Codebase Patterns
- Vault skeleton dirs: fragments/, fragments/discarded/, aspects/, notes/, references/, pieces/
- `resolveProject` in storage-service is the entry point for first project access — good place for startup repairs
- Registry tests use `BASIC_VAULT` fixture (cpSync'd to tmp dir); storage-service tests do the same
- `ensureVaultSkeleton` uses `mkdir(path, { recursive: true })` making it idempotent and safe anywhere
- Fragment rename logic lives in `storage-service.ts` `fragments.write` — captures oldFilePath before write, then deletes old file if key changed
- Use `readdirSync` (not `existsSync`) to check exact on-disk filename casing — `existsSync` is case-insensitive on macOS and will return true for wrong-case paths
- Case-only rename fix: temp-file roundtrip (rename old→temp, write new, unlink temp); works on both case-sensitive (Linux) and case-insensitive (macOS/Windows) filesystems
- Rebuild-state tracking lives in `packages/api/src/middleware/rebuild-state.ts` (module-level Maps/Sets); import `isProjectRebuilding` for status checks, `registerRebuild` for deduplication
- Generated frontend API client is gitignored — always run `bun run codegen` in packages/frontend after adding/changing API endpoints
- To add a new path alias in frontend: update tsconfig.app.json paths, vite.config.ts alias, AND vitest.config.ts alias — all three are required
- Status endpoints that must NOT await rebuild: register in `projectsRouter` (under `/projects` prefix), not in `projectScopedApp` (which uses `resolveProject` middleware)
- Aspect color utilities live in `packages/frontend/src/pages/OverviewPage/utils/aspectColors.ts` — `ASPECT_COLOR_PALETTE` (exported) and `resolveAspectColor(key, explicitColor)` for consistent color resolution everywhere
- `AspectUpdateSchema.color` accepts `null` to explicitly clear the color (falls back to deterministic hash); `undefined` means "don't change the color field"
- `aspect:updated` changedFields enum includes `"color"` — add to it when adding new aspect fields that use the catch-all log type

# Ralph Progress Log
Started: [pending]
---

## 2026-05-22 - US-001
- Implemented full vault skeleton bootstrap on project creation and lazy repair on startup
- Created `packages/storage/src/utils/vault-skeleton.ts` with `VAULT_SKELETON_DIRS` and `ensureVaultSkeleton`
- Updated `packages/storage/src/registry/registry.ts`: replaced single `aspects/` mkdir with `ensureVaultSkeleton(vaultPath)` in create mode
- Updated `packages/storage/src/service/storage-service.ts`: added `ensureVaultSkeleton(vaultPath)` call in `resolveProject` after `cleanupStaleDirectories`
- Updated `packages/storage/src/__tests__/registry.test.ts`: expanded skeleton test to assert all 6 dirs
- Updated `packages/storage/src/__tests__/storage-service.test.ts`: added lazy-repair test using a minimal vault with only `.maskor/`
- Updated `specifications/project-management.md`: listed all skeleton dirs including the new lazy-repair behavior
- **Learnings for future iterations:**
  - BASIC_VAULT has files in `fragments/discarded/` — can't use `rmdirSync` on non-empty dirs; use a purpose-built minimal vault in tests instead
  - The `resolveProject` method is the right hook for startup-time repairs; it runs before any user-facing operation on a project
  - The existing TODO comment in registry.ts (`// TODO: create helper function for creating entire repo structure`) was exactly what US-001 addressed
---

## 2026-05-22 - US-002
- Fixed case-only fragment rename on case-insensitive filesystems (macOS APFS, Windows NTFS)
- Root cause: `fragments.write` in storage-service.ts wrote new content to "chapter one.md", then called `unlink("Chapter One.md")` — on case-insensitive FS both paths are the same physical file, so the content was deleted
- Fix: added `isCaseOnlyRename` detection in `storage-service.ts` `fragments.write`; when triggered, uses temp-file roundtrip (`rename old→temp`, `vault.write new`, `unlink temp`) instead of write-then-unlink
- Added `rename` to imports from `node:fs/promises` in storage-service.ts
- Updated `packages/storage/src/__tests__/storage-service.test.ts`: new describe block "case-only rename" with test verifying UUID/key survival and correct on-disk filename via `readdirSync`
- Updated `specifications/fragment-editor.md`: added Prior decisions entry documenting the case-only rename behavior
- **Learnings for future iterations:**
  - Fragment key is derived from the on-disk filename (basename), not from frontmatter — if the file keeps the old case, the watcher will re-index with old key
  - `existsSync` is case-insensitive on macOS — use `readdirSync` to verify exact filename casing in tests
  - The `isCaseOnlyRename` check: `old !== new && old.toLowerCase() === new.toLowerCase()` correctly fires for case-only changes on both FS types
  - `vault.fragments.write` derives path from `fragment.key` — after `rename old→temp`, writing creates a fresh file at the new-case path on both FS types
---

## 2026-05-22 - US-003
- Fixed concurrent-request bug: changed `resolveProject` middleware from `Set<string>` (rebuilt flag) to `Map<string, Promise<void>>` via new `packages/api/src/middleware/rebuild-state.ts` module. All concurrent requests now await the same rebuild promise instead of the first one marking "done" before the await completes.
- Added `GET /projects/:projectId/rebuild-status` endpoint (in `projectsRouter` — bypasses `resolveProject` middleware). Returns `{ rebuilding: boolean }` immediately without blocking.
- Added `ProjectRebuildStatusSchema` to `packages/api/src/schemas/project.ts`.
- Created `packages/frontend/src/contexts/RebuildStatusContext.tsx`: polls rebuild-status every 500ms while rebuilding; invalidates all project queries on completion.
- Wrapped `<Outlet />` in `ProjectShellLayout.tsx` with `<RebuildStatusProvider>`.
- Updated `FragmentListPage`, `OverviewPage`, and `ProjectConfigPage` to show "Rebuilding project index…" when `isRebuilding && isLoading`.
- Added `@contexts` path alias to tsconfig.app.json, vite.config.ts, and vitest.config.ts.
- Added tests: rebuild-status endpoint returns boolean; concurrent fragment requests all return same non-empty data (covers the race bug).
- Updated `specifications/storage-sync.md` with rebuild-in-progress UX contract.
- **Browser verification not performed** — no browser MCP available; manual verification required.
- **Learnings for future iterations:**
  - The concurrent-request bug: `rebuiltProjects.add(uuid)` ran synchronously BEFORE `await rebuild()`, so concurrent requests skipped the wait. Fix: promise map where all requests await the same promise.
  - `projectsRouter` is mounted at `/projects` without `resolveProject` middleware — perfect for bypass routes. `projectScopedApp` runs `resolveProject` for ALL routes under `/projects/:projectId`.
  - Orval-generated client files are gitignored. The `useGetProjectRebuildStatus` hook is generated from the OpenAPI spec after running `bun run codegen`.
  - Three alias files to update: tsconfig.app.json, vite.config.ts, vitest.config.ts.
---

## 2026-05-25 - US-007
- Added aspect color picker to the aspect editor (constrained 10-color palette)
- Exported `ASPECT_COLOR_PALETTE` from `OverviewPage/utils/aspectColors.ts` for reuse
- Fixed `storage-service.ts` aspects.update to apply `patch.color` (was missing)
- Extended `AspectUpdateSchema.color` to `nullable().optional()` to support explicit color clearing (null = clear, undefined = no change)
- Added `"color"` to `aspect:updated` changedFields enum in `action.ts`
- Added `colorChanged` detection and `aspect:updated` log entry in `update-aspect.ts`
- Updated `fragment-metadata-form.tsx` to show color dots next to aspect names
- Added 3 API tests for color set, clear, and list persistence
- Updated `specifications/aspect-arc-model.md` with color field docs and Prior decision
- **Browser verification not performed** — no browser MCP available; manual verification required
- **Learnings for future iterations:**
  - The storage-service aspect update was missing `color` in the patch spread — always check all optional fields are covered
  - `useLiveFieldSave` with `string | null` serverValue works cleanly for nullable fields; use `null` not `""` for cleared state
  - Sending `null` in the patch for a nullable schema field passes zod validation; `patch.color ?? undefined` converts null→undefined for the Aspect object
  - Pre-existing `fragment-metadata-form.test.tsx` failure unrelated to this story (router mock issue)
---

## 2026-05-25 - US-004
- `Create <entity>…` palette commands now open a creation modal directly instead of navigating to the entity list
- Created `packages/frontend/src/components/global-create-dialogs.tsx`: shell-level component with 4 creation dialogs (fragment, note, reference, aspect). After creation, navigates to the new entity.
- Updated `useProjectShellCommands.ts`: accepts a `createHandlers` parameter with callbacks for each entity type; commands call these callbacks instead of navigating
- Updated `ProjectShellLayout.tsx`: manages `activeCreate` state, passes handlers to `useProjectShellCommands`, renders `GlobalCreateDialogs`
- Updated `specifications/command-palette.md`: documented modal-handoff pattern for create commands
- Updated tests in `useProjectShellCommands.test.tsx`: 4 new tests verify callbacks are invoked; removed old navigate-assertion tests
- **Learnings for future iterations:**
  - `GlobalCreateDialogs` uses if/else rendering (not `Dialog open={false}`) — avoids mounting unnecessary query hooks
  - The modal-handoff pattern is documented in command-palette.md Prior decisions: palette opens modal, modal owns creation, success navigates to new entity
  - `@components` alias already exists; `@tanstack/react-query`'s `useQueryClient` must be imported separately from `@tanstack/react-router`
  - Pre-existing test failure: `fragment-metadata-form.test.tsx` fails due to incomplete router mock — unrelated to this story
---

## 2026-05-25 - US-008
- Implemented aspect delete cascade: deleting an aspect now strips its weight key from all fragment frontmatter atomically and deletes the associated arc file
- Added `cascadeAspectDelete` helper in `packages/storage/src/service/storage-service.ts` (mirrors `cascadeAspectKeyRename` but removes the key rather than renaming it)
- Changed `aspects.delete` return type from `Promise<void>` to `Promise<{ cascadedFragments: string[] }>`
- Updated `packages/api/src/commands/aspects/delete-aspect.ts` to extract cascade result and include `cascadeFragmentCount` in the `aspect:deleted` log payload
- Updated `packages/shared/src/schemas/domain/action.ts`: `aspect:deleted` payload changed from `empty` to `z.object({ cascadeFragmentCount: z.number() })`
- Updated `packages/frontend/src/pages/ProjectHistoryPage/renderers/aspect.tsx`: `aspect:deleted` render now shows cascade count
- Added 5 tests in `packages/storage/src/__tests__/cascade.test.ts` covering: strip on disk, cascade UUIDs returned, empty cascade, arc deletion, no arc (silent)
- Updated `specifications/aspect-arc-model.md`: prior decision and acceptance criteria updated to document cascade-delete as the chosen path
- **Learnings for future iterations:**
  - `cascadeAspectDelete` follows the same pattern as `cascadeAspectKeyRename` — cascade fragments first, then delete the aspect entity and commit in one transaction
  - The arc delete happens before the fragment cascade so any error there aborts before touching fragment files
  - `fragment_aspects` DB rows are cleaned up automatically: `cascadeFragments` re-upserts all touched fragments, and `upsertFragment` re-syncs `fragment_aspects` from the (now-updated) fragment object
  - The `aspect:deleted` schema change is backward-compatible in practice (no existing clients depend on the exact payload shape for this type)
---

## 2026-05-25 - US-009
- Rendered orphaned aspects (aspect keys on a fragment whose project definition was deleted) in the metadata editor with a muted style and "orphaned" badge
- Changed `visibleAspects` → `liveAspects` (filtered to known project aspects) + added `orphanedAspects` (filtered to unknown keys) in `fragment-metadata-form.tsx`
- Orphaned aspects render below live aspects with `opacity-50`, an empty circle (no color dot), the key + weight, an "orphaned" badge, and the same × detach button
- Updated `getAttachedAspects` in the command hook call to include all attached keys (live + orphaned) so palette detach works on orphans too
- Fixed pre-existing test failure in `fragment-metadata-form.test.tsx`: updated the `@tanstack/react-router` mock to use `importOriginal` so all router exports are available (prevents registry.ts import errors)
- Added `CommandsProvider` to the test `wrap` function (required by `useCommands()` / `useFragmentMetadataCommands()`)
- Added 2 new tests: orphaned aspect renders with badge; detaching an orphaned aspect removes it and fires the PATCH
- Updated `specifications/fragment-editor.md`: added Shipped entry and Prior decision documenting orphan rendering
- **Learnings for future iterations:**
  - The `vi.mock("@tanstack/react-router")` in `fragment-metadata-form.test.tsx` was too narrow (only mocked `Link`), causing a suite-level error because `registry.ts` imports `createRootRouteWithContext`. Fix: use `importOriginal` + spread actual exports.
  - Components using `useCommands()` or catalog command hooks require `CommandsProvider` in the test wrapper.
  - Orphan indicator uses `opacity-50` + empty circle (no `backgroundColor`) + `"orphaned"` text badge — consistent with the muted treatment used elsewhere for deprecated/inactive items.
---
