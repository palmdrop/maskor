# Drizzle-kit generated migration snapshots — auto-generated, not hand-edited
**/migrations/meta/*.json

# OpenAPI snapshot — generated by `packages/api generate-openapi`, formatted by the
# generator (JSON.stringify), verified byte-for-byte by the verify:openapi guard.
packages/frontend/src/api/openapi.json

# Hand-authored prose docs (plans, reviews, ADRs, agent skills). `format` runs
# `prettier --write .` over the whole repo, so without this it reflows these markdown
# files (e.g. `*italics*` → `_italics_`, table padding) on every run, churning files
# the current change never touched. Keep them out of both `--write` and `lint`'s `--check`.
.claude/skills/
references/
