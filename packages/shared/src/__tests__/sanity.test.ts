import { describe, it, expect } from "bun:test";

describe("@maskor/shared", () => {
  it("exports are accessible", () => {
    // Dummy: replace with real import assertions once utils exist
    expect(true).toBe(true);
  });

  it("performs basic arithmetic correctly", () => {
    expect(1 + 1).toBe(2);
  });
});
