---
name: run-prompt
description: Read and execute the task described in `.claude/PROMPT.md`. Use this skill whenever the user says something like "Run prompt", "Do the prompt", "Prompt!", "execute the prompt", or any phrasing that refers to running or executing the project prompt file.
---

Read `.claude/PROMPT.md` and carry out the task it describes, exactly as written.

If the file doesn't exist, tell the user.
